# Copia este archivo a .env.dev y pon la MISMA contraseña en secrets/pg_password.txt.
# Ninguno de los dos se versiona: ambos están en .gitignore y .dockerignore.
# El script ./devops.sh docker los genera automáticamente si faltan.
DB_PASSWORD=cambia-esta-contrasena
